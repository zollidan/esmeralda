apiVersion: 1

providers:
  - name: "logs"
    orgId: 1
    folder: "Docker Logs"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
